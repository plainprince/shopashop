# Shopshop

## To install dependencies:

- `bun install`

- download PocketBase
- Create a admin user with the following credentials: test@bot.com, botbotbotbot

## To run:

- start pocketbase
- `bun run index.js`